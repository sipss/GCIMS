% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-GCIMSSample.R
\name{plot,GCIMSSample,ANY-method}
\alias{plot,GCIMSSample,ANY-method}
\title{Topographical plot of a GCIMSSample object}
\usage{
\S4method{plot}{GCIMSSample,ANY}(
  x,
  dt_range = NULL,
  rt_range = NULL,
  ...,
  remove_baseline = FALSE,
  trans = "cubic_root",
  intensity_range = "ranged"
)
}
\arguments{
\item{x}{A \link{GCIMSSample} object}

\item{dt_range}{A numeric vector of length 2 with the drift time range to plot (in milliseconds)}

\item{rt_range}{A numeric vector of length 2 with the retention time range to plot (in seconds)}

\item{...}{Ignored}

\item{remove_baseline}{Set to \code{TRUE} to subtract the estimated baseline first}

\item{trans}{The transformation to the intensity values. "cubic_root" is the default. "intensity" is also valid.
See the \code{trans} argument in \code{\link[ggplot2:continuous_scale]{ggplot2::continuous_scale()}} for other possibilities.}

\item{intensity_range}{Controls the color scale limits. One of:
\itemize{
\item \code{"ranged"} (the default): the range of exactly what's plotted (respecting
\code{dt_range}, \code{rt_range} and \code{remove_baseline}).
\item \code{"global"}: this sample's own full, uncropped, raw intensity range,
regardless of \code{dt_range}/\code{rt_range} -- useful to see how strong a cropped
region is relative to the whole sample. Not valid together with
\code{remove_baseline = TRUE}.
\item A numeric vector of length 2, \code{c(min, max)}: fixed limits. Set this
explicitly to make several plots comparable on the same color scale.
\item A length-2 vector/list whose elements are independently a number,
\code{"global"} or \code{"ranged"}, e.g. \code{list(min = 0, max = "global")}.
}}
}
\value{
A plot of the GCIMSSample
}
\description{
Topographical plot of a GCIMSSample object
}
\examples{
dummy_obj <-GCIMSSample(
  drift_time = 1:2,
  retention_time = 1:3,
  data = matrix(1:6, nrow = 2, ncol = 3),
  gc_column = "Optional column name",
  drift_gas = "nitrogen",
  drift_tube_length = 98.0 # in mm
)
plot(dummy_obj)
}
